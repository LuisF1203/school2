#include <stdio.h>
/*
Primer Ejercicio

Utilizando una única instrucción:

1.Declare c, estaEsUnaVariable, q76895 y numero, como variables de tipo entero

2.Solicite al usuario que introduzca un entero; termine la solicitud con dos puntos ( : ) seguido por un espacio y deje el cursor después del espacio

3.Imprima en una el mensaje "Este es un programa en lenguaje C"

4.Imprima en dos líneas el mensaje "Este es un programa en lenguaje C" terminando la primera línea en la palabra programa

5.Imprima el mensaje "Este es un programa en lenguaje C" dejando cada palabra en una línea diferente

6.Imprima el mensaje "Este es un programa en lenguaje C" separando cada palabra con un tabulador

 

Segundo Ejercicio:

 

Escriba instrucciones (o comentarios) que logren lo siguiente:

1.Indique que el programa calculará el producto de tres enteros

2.Declare x, y, z y resultado como variables de tipo entero

3.Pida al usuario que introduzca tres enteros

4.Asigne los valores 4, 5 y 6 a las variables x, y y z

5.Calcule el producto de los valores contenidos en las variables x, y y z y asigne el resultado a la variable resultado.

6.Imprima en la pantalla "El producto es " seguido del valor de la variable resultado.

7.Devuelva un valor desde main que indique que el programa ha terminado correctamente.
*/
int main(){
    int c;
    int estaEsUnaVariable;
    int q76895;
    int numero;
    scanf("introduce un numero entero:");
    printf("este es un progrma en lenguaje C \n este es un programa en lenguaje C");
    
    printf("este es un progrma \n en lenguaje C");
    printf("este\nes\nun\nprogrma\nen\nlenguaje\nC");
    printf("este\tes\ttun\tprogrma\ten\tlenguaje\tC");
    return 0;
}